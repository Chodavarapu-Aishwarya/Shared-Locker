# Shared-Locker-
 A c++ based shared locker which allows user to configure and use the locker. The encoding and decoding of the locker id and the shared keys among users is done using Chinese Reminder Theorm and GCD principles.
